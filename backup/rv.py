
class RV(metaclass=Meta):
    """
    Return Value Class.

    Examples:
        >>> from rc import pretty_install
        >>> from rc import RV
        >>>
        >>> pretty_install()
        >>>
        >>> r = RV()
        >>> r._rv
        []
        >>> r.rv
    """
    __slots__ = tuple()

